"""Seed the graph with a handful of synthetic failure cases.

So the very first preflight call returns non-empty results. Run via:
  uv run python ../scripts/seed.py
"""

from __future__ import annotations

# TODO(R3): build 5-10 synthetic FailureCase records and persist them
# via aag.graph.writer to populate nodes + edges + embeddings.
