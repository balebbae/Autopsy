"""Detect schema-field-addition change patterns in the run's diffs."""

from __future__ import annotations

# TODO(R3): scan diff.patch for added field declarations in:
#   *.schema.ts, zod schemas, drizzle tables, pydantic models, prisma schema.
