"""Symptom: schema_field_addition present but migrations/** untouched."""

from __future__ import annotations

# TODO(R3): activate only when schema_change.check fired; flag if no diff
# matches r"(?:^|/)migrations?/".
